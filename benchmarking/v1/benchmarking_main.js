/**
 * 
 */

var fs = require('fs');
//var file = '../charting/chart_data.json';
var engine_wrapper = require("../benchmarking/engine_wrapper");

//var fd = fs.openSync(file);
//var variable = [];
//
//for(var i = 0; i < 10; i++){
//	variable[i] = i;
//	fs.appendFileSync(file, JSON.stringify(variable));
//
//}
engine_wrapper.outputEverything();
//console.log("Here's the results from the sort: %j", engine_wrapper.callTailoredObjectFunctions());
//engine_wrapper.writeResults(engine_wrapper.add(), "add");
//engine_wrapper.writeResults(engine_wrapper.add(), "add");

